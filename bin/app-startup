#!/bin/bash

dev() {
  nodemon --exec "app-lint-conditional && app-build && app"
}

prod() {
  app-lint-conditional && \
  app-build && \
  app-test && \
  forever -c app
}

if [ "$NODE_ENV" = "production" ]; then
  prod
else
  dev
fi
